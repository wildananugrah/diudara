references/

# Secrets — never commit. .env.example files are the committed templates.
.env
.env.*
!.env.example

node_modules/
dist/
.DS_Store
*.local

# Uploaded files are user data, not source.
backend/storage/
