# Leave unset for local dev: vite.config.ts proxies /api to 127.0.0.1:3004,
# so the app uses the same "/api" origin-relative base it uses in production.
# Set it only when pointing the SPA at a different host.
# VITE_API_URL=http://127.0.0.1:3004/api
