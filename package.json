{
  "name": "diudara",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "test": "bun run --workspaces test",
    "typecheck": "bun run --workspaces typecheck"
  }
}
