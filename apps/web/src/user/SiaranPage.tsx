/**
 * `/siaran` — Phase 7 re-points live streaming here (see the member-UI spec
 * §6/§8). Until then this is an HONEST placeholder, not a spinner, for the
 * same reason `BerandaPage` gives.
 */
export default function SiaranPage() {
  return (
    <main className="user-page">
      <h1>Siaran</h1>
      <p>Belum ada siaran langsung.</p>
    </main>
  );
}
